{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for vault-tauri Phase 3 boot. Phase 4 extends with memory CRUD command permissions per BRD §11.12.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
